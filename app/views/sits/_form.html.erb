<%= simple_form_for(@sit, :html => { :class => 'form-horizontal'}) do |f| %>
  <% if @sit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sit.errors.count, "error") %> prohibited this sit from being saved:</h2>

      <ul>
      <% @sit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="control-group radio_buttons required"><div class="controls"><label class="radio"><input checked="checked" class="radio_buttons required" id="sit_s_type_0" name="sit[s_type]" type="radio" value="0" />I just sat for <input class="string optional" id="sit_duration" name="sit[duration]" size="50" type="text" value="<%= @user.default_sit_length %> "> minutes</label><br><label class="radio"><input class="radio_buttons required" id="sit_s_type_1" name="sit[s_type]" type="radio" value="1" />This is a journal.</label></div></div>
  
  <div class="new-sit-title">
    <%= f.input :title %>
  </div>

  <div class="new-sit-duration">
    
  </div>

  <div class="new-sit-date">
    <%= f.input :created_at, :as => 'string', :label => 'Date', :input_html => { :placeholder => 'Just now.', :label => 'none', :id => 'datepicker' } %>
  </div>

  <%= f.input :body, :label => 'What\'s happening?', :input_html => { :rows => 10, :class => "basic-form" } %>

  <%= f.input :disable_comments %>

  <%= f.submit %>
  
<% end %>
