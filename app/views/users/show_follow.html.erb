<% provide(:title, @title) %>
<div class="section me">

  <section class="sidebar">
    <%= render :partial => 'shared/sidebar-user' %>
  </section>

  <section class="content">

    <h1><%= @title %></h1>
    <hr>
    
    <% if @users.any? %>
      <div class="user_avatars">
        <% @users.each do |user| %>
          <%= link_to avatar(user), user %>
        <% end %>
      </div>
    <% else %>
      <div class="well">
        <p>No connections yet!</p>
      </div>
    <% end %>
  
  </section>
  
</div>