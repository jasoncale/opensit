<%= large_avatar_of(@user) %>
<div class="sidebar-content">
	<div class="display_name" itemprop="name"><%= username(@user, false) %></div>
  <div class="username" itemprop="alternateName"><%= @user.username %></div>

  <%= render 'users/details' %>

	<%= render 'shared/follow_button', :user => @user %>
</div>
